\documentclass{article}
\usepackage{amsmath}
\author{Dan Ibanez}
\title{Graph Concepts in Flounder}

\begin{document}
\begin{enumerate}

\item A bipartite graph in which all the vertices
on one side have the same degree, but the vertices
on the other side do not.

\item A function whose output graph includes
the input graph's edges as nodes:

\[G'=(V',E')=f(G=(V,E))\]
\[V' = V \cup E\]
\[E = \{(v,e) | (v,u) = e \in E\}\]

\define{enumerate}
\end{document}
